# Synthetic Data

Synthetic data in this folder is ECG-like toy data generated for public smoke
and demo runs.

It is not derived from participants, is not clinically meaningful, and must not
be used to claim reconstruction performance.

Generate a demo dataset with:

```bash
python scripts/make_synthetic_data.py --output data/synthetic/demo
```
